# 실습 — ga4-notion-publisher

```bash
claude --agent ga4-notion-publisher "발송"
# 1) HTML → 노션 페이지 변환
# 2) Discord #marketing-os 에 HTML 첨부 + 요약 embed
# 3) (선택) Gmail 발송
```

## 한 줄 명령으로 3 단계 묶기 (Part 10 미리보기)
```bash
claude /weekly-ga4-report
# → ga4-analyzer → ga4-html-report → ga4-notion-publisher 자동 체이닝
```
